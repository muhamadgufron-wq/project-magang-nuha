import { LoginForm } from "@/modules/auth";

/**
 * Halaman Login (Route Page).
 * File ini hanya bertugas memanggil komponen LoginForm dari modul auth.
 */
export default function LoginPage() {
  return <LoginForm />;
}
